#include "../camera/camera.hpp"
#include "player.hpp"

void Player::update()
{
  // apply position to texture
  //Vec3 abs_position = this->absolute_position();
  //Vec3 window_position = Camera::main()->world_to_window(abs_position);
  //this->sprite->update_position(window_position);
}
