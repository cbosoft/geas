#include "quit_button.hpp"

QuitButton::QuitButton(Transform *parent, const Vec2 &position, const std::string &name, const std::string &font, const std::string &text)
    :   UIElement(parent, position, name, font, text)
{
    // do nothing
}